<p>Version <code>{version}</code> | Identifier: <code>{identifier}</code></p>
<div class="box box-info">
  <div class="box-body">
    <p>Thank you for using {name}.</p>
    <hr>
    <p><strong>Permissions added to subuser system:</strong></p>
    <ul>
      <li><code>split.read</code> — View split servers</li>
      <li><code>split.create</code> — Create a split server</li>
      <li><code>split.update</code> — Update a split server's resources</li>
      <li><code>split.delete</code> — Delete a split server</li>
    </ul>
    <p>Set <strong>Split Limit</strong> on each server's Build Configuration page to enable splitting. A value of <code>0</code> disables splitting.</p>
  </div>
</div>
